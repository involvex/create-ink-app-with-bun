# Plan: Create AGENTS.md for create-ink-app-with-bun

## Analysis

This is a CLI scaffold tool that generates Ink (React for CLI) applications. I found:

- **Build/Test/Lint commands** from package.json and copilot-instructions.md
- **Code style**: XO linting, Prettier, ES modules, TypeScript in templates
- **Existing guidelines**: .github/copilot-instructions.md (90 lines)
- **No existing AGENTS.md** in root directory
- **No Cursor rules** (.cursor/rules/ or .cursorrules)

## AGENTS.md Content (~150 lines)

### 1. Build/Lint/Test Commands

- **Dev**: `bun run dev` or `node cli.js`
- **Test**: `npm test` (runs `xo && ava`)
- **Single test**: `npx ava test.js`
- **Lint**: `npx xo` (templates/\*\* excluded)
- **Format**: `npx prettier --write .`

### 2. Code Style Guidelines

- **Imports**: ES modules with extension (`.js`), named imports preferred
- **Formatting**: XO + Prettier, 2-space indent
- **Types**: TypeScript in templates (bun/ts), JSDoc or inline types in .js files
- **Naming**: camelCase functions/variables, PascalCase components, SCREAMING_SNAKE constants
- **Error handling**: try/catch with graceful degradation, `task.skip()` for optional failures
- **Architecture**: Entry in cli.js → core in index.js → templates/

### 3. Include Copilot Rules

Merge content from `.github/copilot-instructions.md` into AGENTS.md since it provides useful context for agentic tools.

### 4. Repository-Specific Conventions

- Template placeholders: `%NAME%`, `%AUTHOR%`, `%LICENSE%`
- Underscore file convention: `_gitignore` → `.gitignore`
- Listr task flow for scaffolding steps

## Implementation

Write AGENTS.md to `D:\repos\create-ink-app-with-bun\AGENTS.md` with ~150 lines covering all categories above.
