# Plan: AddCLI Flags and Fix TypeScript Errors

## Issue 1: CLI Flags for Install/Git Skip

The flags `--no-install` and `--no-git-init` already exist in `cli.js:59-66`. They're working correctly - no changes needed.

## Issue 2: TypeScript Errors in Bun Template

### Required Fixes:

1. **Add `@types/node`** to `templates/bun/_package.json` devDependencies
   - Fixes `TS2591: Cannot find name 'process'`

2. **Fix tsconfig.json** - Change `moduleResolution` from `NodeNext` to `bundler`
   - Fixes 10x `TS2835: Relative import paths need explicit file extensions`
   - Location: `templates/bun/tsconfig.json:5`

3. **Fix React.React.ReactNode** in `templates/bun/src/app.tsx:25`
   - Change to `React.ReactNode` or import directly

4. **Add type annotation** for `item` parameter in `app.tsx:107` and `commands/settings.tsx:38`

5. **Handle non-standard Text props** (`font`, `alignText`)
   - Create a type definition file that extends Ink's Text component
   - Or remove the props if ink-big-text isn't selected

6. **Fix ESLint config** - The `react-hooks` plugin is imported but there's a configuration issue with how it's set up. Either remove the rules or ensure proper setup.

### Files to Modify:

| File                               | Changes                              |
| ---------------------------------- | ------------------------------------ |
| `templates/bun/_package.json`      | Add `@types/node`                    |
| `templates/bun/tsconfig.json`      | `NodeNext` → `bundler`               |
| `templates/bun/src/app.tsx`        | Fix React type, add item types       |
| `templates/bun/src/cli.tsx`        | Add `.js` extensions                 |
| `templates/bun/src/commands/*.tsx` | Add `.js` extensions, fix Text props |
| `templates/bun/eslint.config.mjs`  | Remove or fix react-hooks rules      |

## Implementation Steps

1. Edit `templates/bun/_package.json` - Add `@types/node` to devDependencies
2. Edit `templates/bun/tsconfig.json` - Change moduleResolution to `bundler`
3. Edit `templates/bun/src/app.tsx` - Fix React.React.ReactNode, add type to item param
4. Edit `templates/bun/src/cli.tsx` - Add `.js` extensions to imports
5. Edit `templates/bun/src/commands/exit.tsx` - Add import for process or use @types/node
6. Edit all command files - Add `.js` extensions + fix/remove font/alignText props
7. Edit or simplify `templates/bun/eslint.config.mjs` - Remove react-hooks rules
